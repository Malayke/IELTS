# IELTS

# env
```
pip install paddlepaddle -i https://mirror.baidu.com/pypi/simple
pip install pip -U
pip install pyenchant paddleocr
HOMEBREW_NO_AUTO_UPDATE=1 HOMEBREW_NO_BOTTLE_SOURCE_FALLBACK=1 brew install enchant
```
